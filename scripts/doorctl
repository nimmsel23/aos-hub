#!/usr/bin/env bash
# doorctl wrapper - delegates to door/cli/doorctl
# Keep this for backwards compatibility

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")" && pwd)"
ROOT_DIR="$(cd "$SCRIPT_DIR/.." && pwd)"

exec "$ROOT_DIR/door/cli/doorctl" "$@"
