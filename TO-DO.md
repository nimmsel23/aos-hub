nodectl bzw indexctl spielen den falschen systemd service zur index-node ein (wurde mir bestätigt durch manuelles starten von cd ~/aos-hub/index-node && node server.js)
aos-index-dev.service ist glaube ich legacy, der aktuelle solle nicht AlphaOS in der beschreibung sondern mit dem greek symbol statt lateinischer Buchstaben.

wir sollten in aos-hub ein bin directory einrichten für alle gängigen ausführbaren skripte und die scripte anordnung dort logischer machen

hyprpolkitagent läuft noch nicht so richtig glaube ich 
hyprpaper zeigt auch keinen hintergrund an in hypr
(startet via kde login screen, unten auswahl:<Hyprland (uwsm-managed)>)

yay -S python-aiohttp-oauth nutzen statt token für gas auth? 


clientctx muss eindeutig 8788 Klientenverwaltung sein

4100 ist wohl smart für tailscale funnel root /
