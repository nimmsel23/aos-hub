#!/usr/bin/env bash
set -euo pipefail

APP="aos"
SCRIPT_DIR="$(cd "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")" && pwd)"

# Normalize to repo checkout if available
if [[ "$SCRIPT_DIR" != "$HOME/aos-hub" && -d "$HOME/aos-hub/scripts" ]]; then
  SCRIPT_DIR="$HOME/aos-hub"
fi

# Pillar roots (explicit, stable paths inside aos-hub)
PILLAR_CORE4_DIR="$SCRIPT_DIR/core4"
PILLAR_VOICE_DIR="$SCRIPT_DIR/voice"
PILLAR_DOOR_DIR="$SCRIPT_DIR/door"
PILLAR_GAME_DIR="$SCRIPT_DIR/game"

have() { command -v "$1" >/dev/null 2>&1; }
die() { echo "[$APP] $*" >&2; exit 1; }
can_prompt() {
  [[ -t 0 && -t 1 ]]
}

resolve_bin() {
  local primary="$1"; shift
  if [[ -n "$primary" && -x "$primary" ]]; then
    echo "$primary"; return 0
  fi
  for cand in "$@"; do
    if [[ -n "$cand" && -x "$cand" ]]; then
      echo "$cand"; return 0
    fi
  done
  if have "$primary"; then
    command -v "$primary"; return 0
  fi
  return 1
}

AOSCTL="$(resolve_bin "$SCRIPT_DIR/aosctl" || true)"
HUBCTL="$(resolve_bin "$SCRIPT_DIR/scripts/hubctl" || true)"
CORE4CTL="$(resolve_bin "$PILLAR_CORE4_DIR/python-core4/core4ctl" "$SCRIPT_DIR/scripts/core4ctl" || true)"
C4D_BIN="$(resolve_bin "c4d" "$HOME/bin/c4d" || true)"
SYNCCTL="$(resolve_bin "$SCRIPT_DIR/scripts/syncctl" || true)"
GITCTL="$(resolve_bin "$SCRIPT_DIR/scripts/gitctl" || true)"
INDEXCTL="$(resolve_bin "$SCRIPT_DIR/scripts/indexctl" || true)"
NODECTL="$(resolve_bin "$SCRIPT_DIR/scripts/nodectl" || true)"
GAMECTL="$(resolve_bin "$PILLAR_GAME_DIR/gamectl" "$SCRIPT_DIR/scripts/gamectl" || true)"
DOORCTL="$(resolve_bin "$PILLAR_DOOR_DIR/cli/doorctl" "$SCRIPT_DIR/scripts/doorctl" || true)"
BACKUPCTL="$(resolve_bin "$SCRIPT_DIR/scripts/backupctl" || true)"
MOUNTCTL="$(resolve_bin "$SCRIPT_DIR/scripts/mountctl" || true)"
HOOKCTL="$(resolve_bin "$SCRIPT_DIR/scripts/hookctl" || true)"
VOICECTL="$(resolve_bin "$PILLAR_VOICE_DIR/voicectl" "$SCRIPT_DIR/scripts/voicectl" || true)"
MOBILEAPPCTL="$(resolve_bin "$SCRIPT_DIR/scripts/mobileappctl" || true)"
GASCTL="$(resolve_bin "$SCRIPT_DIR/scripts/gasctl" || true)"
TELECTL="$(resolve_bin "$SCRIPT_DIR/scripts/telectl" || true)"
FIRECTL="$(resolve_bin "$PILLAR_GAME_DIR/fire/firectl" "$SCRIPT_DIR/scripts/firectl" || true)"
ROUTERCTL="$(resolve_bin "$SCRIPT_DIR/routerctl" "$SCRIPT_DIR/router/routerctl" || true)"
BRIDGECTL="$(resolve_bin "$SCRIPT_DIR/bridgectl" "$SCRIPT_DIR/bridge/bridgectl" || true)"
NODE_RESTART="$SCRIPT_DIR/scripts/ctl/node-restart.sh"
AOS_DASHBOARD_EXT="${AOS_DASHBOARD_EXT:-$SCRIPT_DIR/scripts/aos-dashboard.sh}"

pick_exec_token() {
  local resolved="$1"
  local fallback="$2"
  if [[ -n "$resolved" ]]; then
    printf "%s\n" "$resolved"
  else
    printf "%s\n" "$fallback"
  fi
}

exec_or_path() {
  local resolved="$1"
  local fallback="$2"
  shift 2
  if [[ -n "$resolved" ]]; then
    exec "$resolved" "$@"
  fi
  have "$fallback" || die "$fallback not found in PATH"
  exec "$fallback" "$@"
}

usage() {
  cat <<EOF
$APP — αOS main terminal frontdoor (user workflows + wrappers)

Usage:
  $APP dashboard                    # hotkey terminal dashboard (default in TTY)
  $APP menu                         # gum menu (legacy/simple launcher)
  $APP status|ping [mode]           # quick frontdoor health wrappers
  $APP doctor [local|full]          # frontdoor self-check (+ optional hub doctor)
  $APP restart [all|index|router|bridge]
  $APP sync|vault|git ...           # syncctl/gitctl wrappers
  $APP hub|index|bridge|router ...  # aos-hub system/ops wrappers
  $APP node|nodectl ...             # nodectl compatibility toolbox
  $APP core4|game|door|fire|tele ...  # user/centre wrappers
  $APP hot ...                        # Door Hot List direct frontdoor (via doorctl)
  $APP frame|freedom|focus|tent ...   # Game centre direct frontdoors (via gamectl)
  $APP potential|plan|production|profit ...  # Door 4P phase frontdoors
  $APP backup|mount|hook|gas|voice|mobileapp ...
  $APP aosctl ...                   # direct aosctl passthrough
  $APP mobileappctl|pwa ...         # mobileapp aliases (compat)
  $APP alias <subcommand> [...]     # multi-shell alias manager (aos-alias)

Dashboard:
  - Hotkeys launch common status/doctor/menu flows (gamectl, doorctl, hubctl, ...)
  - Extend/override keys via: ${AOS_DASHBOARD_EXT}
    API in extension file: aos_dashboard_register <key> <group> <label> <desc> <cmd> [args...]

Examples:
  $APP
  $APP dashboard
  $APP game centres
  $APP frame new
  $APP focus list
  $APP door health
  $APP hot list
  $APP hot add "Idea ..."
  $APP potential
  $APP plan activate 1
  $APP production focus
  $APP profit review
  $APP status
  $APP ping
  $APP ping local
  $APP ping remote
  $APP doctor
  $APP doctor full
  $APP hub doctor
  $APP menu
  $APP sync summary
  $APP vault status
  $APP aosctl status
EOF
}

cmd="${1:-}"
shift || true

# ---- launcher menu (gum/fzf/select) ----
aos_menu_pick_select() {
  local prompt="$1"
  shift || true
  local -a options=("$@")
  [[ ${#options[@]} -gt 0 ]] || return 1
  can_prompt || return 1

  printf '%s\n' "$prompt"
  local opt=""
  select opt in "${options[@]}"; do
    [[ -n "$opt" ]] || continue
    printf '%s\n' "$opt"
    return 0
  done
  return 1
}

aos_menu_pick() {
  local prompt="$1"
  shift || true
  local -a options=("$@")
  local backend="${AOS_MENU_BACKEND:-gum}"
  [[ ${#options[@]} -gt 0 ]] || return 1

  if [[ "$backend" == "fzf" ]]; then
    if have fzf && can_prompt; then
      printf '%s\n' "${options[@]}" | fzf --prompt="${prompt} > " --height=~60% --border
      return 0
    fi
    if have gum && can_prompt; then
      gum choose --header "$prompt" "${options[@]}"
      return 0
    fi
  else
    if have gum && can_prompt; then
      gum choose --header "$prompt" "${options[@]}"
      return 0
    fi
    if have fzf && can_prompt; then
      printf '%s\n' "${options[@]}" | fzf --prompt="${prompt} > " --height=~60% --border
      return 0
    fi
  fi

  aos_menu_pick_select "$prompt" "${options[@]}"
}

menu() {
  if ! can_prompt; then
    usage
    return 0
  fi
  if ! have gum && ! have fzf; then
    if [[ -n "$AOSCTL" ]]; then
      exec "$AOSCTL" menu
    elif [[ -n "$HUBCTL" ]]; then
      exec "$HUBCTL" menu
    fi
    die "no gum/fzf found and no aosctl/hubctl menu fallback available"
  fi

  local choice
  local -a menu_options=(
    "Dashboard: hotkey terminal dashboard"
    "Sync: menu (syncctl)"
    "Sync: list targets"
    "Git: status"
    "Vault: status"
    "Core4: dashboard (c4d)"
    "Game: dashboard (gamectl)"
    "Door: dashboard (doorctl)"
    "Hub: menu (hubctl)"
    "Router: control bot (routerctl)"
    "Aliases: manage (aos-alias)"
    "Help"
    "Quit"
  )
  choice="$(aos_menu_pick "αOS — aos" "${menu_options[@]}")"

  case "$choice" in
    "Dashboard: hotkey terminal dashboard")
      dashboard
      ;;
    "Sync: menu (syncctl)")
      if [[ -n "$SYNCCTL" ]]; then
        exec "$SYNCCTL" menu
      fi
      exec syncctl menu
      ;;
    "Sync: list targets")
      if [[ -n "$SYNCCTL" ]]; then
        exec "$SYNCCTL" list
      fi
      exec syncctl list
      ;;
    "Git: status")
      if [[ -n "$GITCTL" ]]; then
        exec "$GITCTL" status
      fi
      exec gitctl status
      ;;
    "Vault: status")
      if [[ -n "$SYNCCTL" ]]; then
        exec "$SYNCCTL" vault status
      fi
      exec syncctl vault status
      ;;
    "Core4: dashboard (c4d)")
      if [[ -n "$C4D_BIN" ]]; then
        exec "$C4D_BIN"
      fi
      core4_cmd menu
      ;;
    "Game: dashboard (gamectl)")
      game_cmd dashboard
      ;;
    "Door: dashboard (doorctl)")
      door_cmd dashboard
      ;;
    "Hub: menu (hubctl)")
      hub_cmd menu
      ;;
    "Router: control bot (routerctl)")
      exec_or_path "$ROUTERCTL" routerctl
      ;;
    "Aliases: manage (aos-alias)")
      exec aos-alias ui
      ;;
    "Help") usage ;;
    "Quit") exit 0 ;;
    *) exit 0 ;;
  esac
}

# ---- dashboard registry ----
declare -a AOS_DASH_KEYS=()
declare -A AOS_DASH_GROUP=()
declare -A AOS_DASH_LABEL=()
declare -A AOS_DASH_DESC=()
declare -A AOS_DASH_CMD=()
AOS_DASH_SEP=$'\037'
AOS_DASH_READY=0
AOS_DASH_MESSAGE=""
AOS_DASH_MESSAGE_LEVEL="info"
AOS_DASH_SCREEN_ACTIVE=0

aos_dashboard_register() {
  local key="${1:-}"
  local group="${2:-General}"
  local label="${3:-}"
  local desc="${4:-}"
  shift 4 || true

  [[ -n "$key" ]] || die "dashboard register: missing key"
  [[ ${#key} -eq 1 ]] || die "dashboard register: key must be 1 char ($key)"
  [[ -n "$label" ]] || die "dashboard register: missing label for key $key"
  [[ $# -ge 1 ]] || die "dashboard register: missing command for key $key"

  local packed="$1"
  shift || true
  local arg
  for arg in "$@"; do
    packed+="${AOS_DASH_SEP}${arg}"
  done

  if [[ -z "${AOS_DASH_LABEL[$key]+x}" ]]; then
    AOS_DASH_KEYS+=("$key")
  fi
  AOS_DASH_GROUP[$key]="$group"
  AOS_DASH_LABEL[$key]="$label"
  AOS_DASH_DESC[$key]="$desc"
  AOS_DASH_CMD[$key]="$packed"
}

dashboard_unpack_cmd() {
  local key="$1"
  local packed="${AOS_DASH_CMD[$key]:-}"
  AOS_DASH_CMDV=()
  [[ -n "$packed" ]] || return 1
  IFS="$AOS_DASH_SEP" read -r -a AOS_DASH_CMDV <<<"$packed"
  [[ ${#AOS_DASH_CMDV[@]} -gt 0 ]]
}

dashboard_command_exists() {
  local key="$1"
  dashboard_unpack_cmd "$key" || return 1
  local bin="${AOS_DASH_CMDV[0]}"
  if [[ "$bin" == */* ]]; then
    [[ -x "$bin" ]]
  else
    have "$bin"
  fi
}

dashboard_set_message() {
  AOS_DASH_MESSAGE_LEVEL="${1:-info}"
  AOS_DASH_MESSAGE="${2:-}"
}

dashboard_screen_enter() {
  [[ "$AOS_DASH_SCREEN_ACTIVE" == "1" ]] && return 0
  [[ -t 1 ]] || return 0
  [[ "${TERM:-dumb}" != "dumb" ]] || return 0
  printf '\033[?1049h\033[2J\033[H\033[?25l'
  AOS_DASH_SCREEN_ACTIVE=1
}

dashboard_screen_leave() {
  [[ "$AOS_DASH_SCREEN_ACTIVE" == "1" ]] || return 0
  printf '\033[?25h\033[?1049l'
  AOS_DASH_SCREEN_ACTIVE=0
}

dashboard_screen_show_cursor() {
  [[ "$AOS_DASH_SCREEN_ACTIVE" == "1" ]] || return 0
  printf '\033[?25h'
}

dashboard_screen_hide_cursor() {
  [[ "$AOS_DASH_SCREEN_ACTIVE" == "1" ]] || return 0
  printf '\033[?25l'
}

dashboard_http_code() {
  local url="$1"
  if ! have curl; then
    printf "curl?"
    return 0
  fi
  local code
  code="$(curl -sS -o /dev/null -m 0.4 -w "%{http_code}" "$url" 2>/dev/null || true)"
  if [[ -z "$code" || "$code" == "000" ]]; then
    printf "down"
  else
    printf "%s" "$code"
  fi
}

dashboard_svc_state() {
  local scope="$1" unit="$2"
  if ! have systemctl; then
    printf "no-systemctl"
    return 0
  fi
  local out=""
  if [[ "$scope" == "user" ]]; then
    out="$(systemctl --user is-active "$unit" 2>/dev/null || true)"
  else
    out="$(systemctl is-active "$unit" 2>/dev/null || true)"
  fi
  if [[ -n "$out" ]]; then
    printf "%s" "$out"
  else
    printf "unavail"
  fi
}

dashboard_mark() {
  local key="$1"
  if dashboard_command_exists "$key"; then
    printf "ok"
  else
    printf "!!"
  fi
}

dashboard_term_size() {
  AOS_DASH_ROWS=24
  AOS_DASH_COLS=80

  local sz=""
  if [[ -t 0 ]]; then
    sz="$(stty size 2>/dev/null || true)"
  elif [[ -r /dev/tty && -w /dev/tty ]]; then
    sz="$(stty size </dev/tty 2>/dev/tty || true)"
  fi

  if [[ "$sz" =~ ^([0-9]+)[[:space:]]+([0-9]+)$ ]]; then
    AOS_DASH_ROWS="${BASH_REMATCH[1]}"
    AOS_DASH_COLS="${BASH_REMATCH[2]}"
    return 0
  fi

  if have tput; then
    local lines cols
    lines="$(tput lines 2>/dev/null || true)"
    cols="$(tput cols 2>/dev/null || true)"
    [[ "$lines" =~ ^[0-9]+$ ]] && AOS_DASH_ROWS="$lines"
    [[ "$cols" =~ ^[0-9]+$ ]] && AOS_DASH_COLS="$cols"
  fi
}

dashboard_move_to() {
  local row="$1" col="${2:-1}"
  printf '\033[%d;%dH' "$row" "$col"
}

dashboard_print_line_fit() {
  local line="${1:-}"
  local width="${AOS_DASH_COLS:-80}"
  (( width < 8 )) && width=8
  if ((${#line} > width)); then
    if (( width > 3 )); then
      line="${line:0:width-3}..."
    else
      line="${line:0:width}"
    fi
  fi
  printf '\033[2K%s\n' "$line"
}

dashboard_render_body_content() {
  local now
  now="$(date '+%Y-%m-%d %H:%M:%S')"
  printf 'aos  |  terminal frontdoor  |  %s\n' "$now"
  printf '\n'
  printf '[Portals]\n'
  printf '  [c] Core4   (%s) -> c4d (fallback: core4ctl menu)\n' "$(dashboard_mark c)"
  printf '  [g] Game    (%s) -> gamectl dashboard\n' "$(dashboard_mark g)"
  printf '  [d] Door    (%s) -> doorctl dashboard\n' "$(dashboard_mark d)"
  printf '  [v] Voice   (%s) -> voicectl menu\n' "$(dashboard_mark v)"
  printf '\n'
  printf '[Layers]\n'
  printf '  [h] hubctl    (%s) -> system/service layer (router/bridge/node/sync/...)\n' "$(dashboard_mark h)"
  printf '  [a] aosctl    (%s) -> /opt/aos system service layer\n' "$(dashboard_mark a)"
  printf '  [m] aos menu  (local launcher / command palette)\n'
  printf '\n'
  printf '[Principle]\n'
  printf '  aos = frontdoor / portals. Component actions live in the wrapped *ctl CLIs.\n'
  printf '\n'
  printf 'Press ? for advanced keymap. Extension file: %s\n' "$AOS_DASHBOARD_EXT"
}

dashboard_help_keymap() {
  local key group prev_group="" state_mark
  printf '\nKeymap (all registered actions)\n'
  printf '%s\n' '------------------------------'
  for key in "${AOS_DASH_KEYS[@]}"; do
    group="${AOS_DASH_GROUP[$key]}"
    if [[ "$group" != "$prev_group" ]]; then
      [[ -n "$prev_group" ]] && printf '\n'
      printf '[%s]\n' "$group"
      prev_group="$group"
    fi
    if dashboard_command_exists "$key"; then
      state_mark="ok"
    else
      state_mark="!!"
    fi
    printf '  [%s] %-18s  (%s) %s\n' \
      "$key" "${AOS_DASH_LABEL[$key]}" "$state_mark" "${AOS_DASH_DESC[$key]}"
  done
}

dashboard_register_defaults() {
  aos_dashboard_register "a" "Core" "aosctl status" "Unified status + heartbeat ping" \
    "$(pick_exec_token "$AOSCTL" "aosctl")" status
  aos_dashboard_register "A" "Core" "aosctl menu" "Interactive aosctl menu" \
    "$(pick_exec_token "$AOSCTL" "aosctl")" menu
  aos_dashboard_register "z" "Core" "aosctl dashboard" "Live aosctl dashboard (5s)" \
    "$(pick_exec_token "$AOSCTL" "aosctl")" dashboard 5
  aos_dashboard_register "h" "Core" "hubctl menu" "System/service layer launcher" \
    "$(pick_exec_token "$HUBCTL" "hubctl")" menu
  aos_dashboard_register "m" "Core" "aos menu" "Launcher menu (gum/fzf/select)" \
    "$(pick_exec_token "$SCRIPT_DIR/aos" "aos")" menu
  if [[ -n "$C4D_BIN" ]]; then
    aos_dashboard_register "c" "Core" "C4D" "core4 dashboard shortcut (c4d)" \
      "$C4D_BIN"
  else
    aos_dashboard_register "c" "Core" "Core4 menu" "core4ctl menu" \
      "$(pick_exec_token "$CORE4CTL" "core4ctl")" menu
  fi
  aos_dashboard_register "C" "Core" "Core4 status" "core4ctl status" \
    "$(pick_exec_token "$CORE4CTL" "core4ctl")" status

  aos_dashboard_register "i" "Services" "Index status" "hubctl index status" \
    "$(pick_exec_token "$HUBCTL" "hubctl")" index status
  aos_dashboard_register "r" "Services" "Router status" "hubctl router status" \
    "$(pick_exec_token "$HUBCTL" "hubctl")" router status
  aos_dashboard_register "b" "Services" "Bridge health" "hubctl bridge health" \
    "$(pick_exec_token "$HUBCTL" "hubctl")" bridge health
  aos_dashboard_register "u" "Services" "Dev status" "hubctl dev status" \
    "$(pick_exec_token "$HUBCTL" "hubctl")" dev status

  aos_dashboard_register "g" "Centres" "Game dashboard" "gamectl dashboard" \
    "$(pick_exec_token "$GAMECTL" "gamectl")" dashboard
  aos_dashboard_register "G" "Centres" "Game centres" "gamectl centres --brief" \
    "$(pick_exec_token "$GAMECTL" "gamectl")" centres --brief
  aos_dashboard_register "d" "Centres" "Door dashboard" "doorctl dashboard" \
    "$(pick_exec_token "$DOORCTL" "doorctl")" dashboard
  aos_dashboard_register "D" "Centres" "Door hitlist" "doorctl hitlist" \
    "$(pick_exec_token "$DOORCTL" "doorctl")" hitlist
  aos_dashboard_register "w" "Centres" "Door health" "doorctl health" \
    "$(pick_exec_token "$DOORCTL" "doorctl")" health
  aos_dashboard_register "f" "Centres" "Fire status" "hubctl fire status" \
    "$(pick_exec_token "$HUBCTL" "hubctl")" fire status

  aos_dashboard_register "s" "Tools" "Sync summary" "syncctl summary" \
    "$(pick_exec_token "$SYNCCTL" "syncctl")" summary
  aos_dashboard_register "v" "Tools" "Voice menu" "voicectl menu" \
    "$(pick_exec_token "$VOICECTL" "voicectl")" menu
  aos_dashboard_register "V" "Tools" "Voice stats" "voicectl stats" \
    "$(pick_exec_token "$VOICECTL" "voicectl")" stats
  aos_dashboard_register "S" "Tools" "Vault status" "syncctl vault status" \
    "$(pick_exec_token "$SYNCCTL" "syncctl")" vault status
  aos_dashboard_register "x" "Tools" "Git status" "gitctl status" \
    "$(pick_exec_token "$GITCTL" "gitctl")" status
  aos_dashboard_register "o" "Tools" "Backup status" "backupctl status" \
    "$(pick_exec_token "$BACKUPCTL" "backupctl")" status
  aos_dashboard_register "k" "Tools" "Hook status" "hookctl status" \
    "$(pick_exec_token "$HOOKCTL" "hookctl")" status
  aos_dashboard_register "T" "Tools" "Tele help" "telectl help" \
    "$(pick_exec_token "$TELECTL" "telectl")" help
}

dashboard_load_extensions() {
  if [[ -r "$AOS_DASHBOARD_EXT" ]]; then
    # shellcheck disable=SC1090
    source "$AOS_DASHBOARD_EXT"
    dashboard_set_message info "Loaded dashboard extensions: $AOS_DASHBOARD_EXT"
  fi
}

dashboard_init() {
  [[ "$AOS_DASH_READY" == "1" ]] && return 0
  dashboard_register_defaults
  dashboard_load_extensions
  AOS_DASH_READY=1
}

dashboard_render() {
  dashboard_term_size
  printf '\033[2J\033[H'

  local footer_lines=3
  local body_max=$((AOS_DASH_ROWS - footer_lines))
  (( body_max < 3 )) && body_max=3

  local -a body_lines=()
  mapfile -t body_lines < <(dashboard_render_body_content)

  local i
  for ((i = 0; i < body_max && i < ${#body_lines[@]}; i++)); do
    dashboard_print_line_fit "${body_lines[$i]}"
  done

  for ((; i < body_max; i++)); do
    printf '\033[2K\n'
  done

  dashboard_move_to $((AOS_DASH_ROWS - footer_lines + 1)) 1
  printf '\033[2K'
  printf '%*s\n' "${AOS_DASH_COLS}" '' | tr ' ' '-'
  dashboard_print_line_fit 'Hotkeys: [c g d v] portals  [h] hubctl  [a] aosctl  [m] menu  [?] advanced  [q] quit'
  if [[ -n "$AOS_DASH_MESSAGE" ]]; then
    dashboard_print_line_fit "[$AOS_DASH_MESSAGE_LEVEL] $AOS_DASH_MESSAGE"
  else
    dashboard_print_line_fit "Ready."
  fi
}

dashboard_help_overlay() {
  printf '\033[2J\033[H'
  cat <<EOF
aos dashboard help

Built-ins:
  q         Quit dashboard
  ENTER     Refresh dashboard
  ?         This help

Extension API:
  Add file: ${AOS_DASHBOARD_EXT}
  Function: aos_dashboard_register <key> <group> <label> <desc> <cmd> [args...]

Example:
  aos_dashboard_register "w" "Custom" "watchdog" "Trigger local systemstatus" watchdog

Notes:
  - Reuse an existing key to override an entry.
  - New keys append to the end of their group order (in registration order).
  - Commands run in the current terminal and return to the dashboard when they exit.
EOF
  dashboard_help_keymap
  printf '\nPress ENTER to return... '
  dashboard_screen_show_cursor
  IFS= read -r _
  dashboard_screen_hide_cursor
}

dashboard_run_key() {
  local key="$1"
  if ! dashboard_unpack_cmd "$key"; then
    dashboard_set_message err "No command registered for key '$key'"
    return 1
  fi

  local cmdline=""
  local token
  for token in "${AOS_DASH_CMDV[@]}"; do
    printf -v token '%q' "$token"
    cmdline+="${cmdline:+ }${token}"
  done

  printf '\033[2J\033[H'
  printf 'aos dashboard -> [%s] %s\n' "$key" "${AOS_DASH_LABEL[$key]}"
  printf '%s\n' "${AOS_DASH_DESC[$key]}"
  printf '\n$ %s\n\n' "$cmdline"

  dashboard_screen_show_cursor
  set +e
  "${AOS_DASH_CMDV[@]}"
  local rc=$?
  set -e
  dashboard_screen_hide_cursor

  dashboard_set_message info "Key '$key' finished with exit code $rc"
  printf '\n[exit=%d] Press ENTER to return... ' "$rc"
  dashboard_screen_show_cursor
  IFS= read -r _
  dashboard_screen_hide_cursor
  return 0
}

dashboard() {
  if ! can_prompt; then
    usage
    return 0
  fi

  dashboard_init
  dashboard_screen_enter
  trap 'dashboard_screen_leave' EXIT INT TERM

  while true; do
    dashboard_render
    IFS= read -rsn1 key || break
    case "${key:-}" in
      q) break ;;
      "") dashboard_set_message info "Refreshed dashboard" ;;
      "?") dashboard_help_overlay ;;
      $'\e')
        IFS= read -rsn2 -t 0.02 _ || true
        dashboard_set_message info "Use dashboard hotkeys (single key) or ? for help"
        ;;
      *)
        dashboard_run_key "$key" || true
        ;;
    esac
  done

  dashboard_screen_leave
  trap - EXIT INT TERM
}

# ---- wrappers ----
sync_cmd() {
  local sub="${1:-menu}"; shift || true
  exec_or_path "$SYNCCTL" syncctl "$sub" "$@"
}

git_cmd() {
  local sub="${1:-status}"; shift || true
  exec_or_path "$GITCTL" gitctl "$sub" "$@"
}

vault_cmd() {
  local sub="${1:-status}"; shift || true
  exec_or_path "$SYNCCTL" syncctl vault "$sub" "$@"
}

router_cmd() {
  local sub="${1:-status}"; shift || true
  exec_or_path "$ROUTERCTL" routerctl "$sub" "$@"
}

bridge_cmd() {
  local sub="${1:-health}"; shift || true
  exec_or_path "$BRIDGECTL" bridgectl "$sub" "$@"
}

index_cmd() {
  local sub="${1:-status}"; shift || true
  exec_or_path "$INDEXCTL" indexctl "$sub" "$@"
}

node_cmd() {
  local sub="${1:-status}"; shift || true
  exec_or_path "$NODECTL" nodectl "$sub" "$@"
}

hub_cmd() {
  local sub="${1:-menu}"; shift || true
  exec_or_path "$HUBCTL" hubctl "$sub" "$@"
}

aosctl_cmd() {
  local sub="${1:-dashboard}"; shift || true
  exec_or_path "$AOSCTL" aosctl "$sub" "$@"
}

core4_cmd() {
  local sub="${1:-menu}"; shift || true
  exec_or_path "$CORE4CTL" core4ctl "$sub" "$@"
}

game_cmd() {
  local sub="${1:-dashboard}"; shift || true
  exec_or_path "$GAMECTL" gamectl "$sub" "$@"
}

frame_cmd() {
  local sub="${1:-help}"; shift || true
  game_cmd frame "$sub" "$@"
}

freedom_cmd() {
  local sub="${1:-help}"; shift || true
  game_cmd freedom "$sub" "$@"
}

focus_centre_cmd() {
  local sub="${1:-help}"; shift || true
  game_cmd focus "$sub" "$@"
}

tent_centre_cmd() {
  local sub="${1:-help}"; shift || true
  game_cmd tent "$sub" "$@"
}

door_cmd() {
  local sub="${1:-dashboard}"; shift || true
  exec_or_path "$DOORCTL" doorctl "$sub" "$@"
}

hot_cmd() {
  local sub="${1:-list}"; shift || true
  door_cmd hot "$sub" "$@"
}

fire_cmd() {
  local sub="${1:-status}"; shift || true
  exec_or_path "$FIRECTL" firectl "$sub" "$@"
}

door_phase_cmd() {
  local phase="${1:?}"
  shift || true
  door_cmd "$phase" "$@"
}

tele_cmd() {
  local sub="${1:-help}"; shift || true
  exec_or_path "$TELECTL" telectl "$sub" "$@"
}

backup_cmd() {
  local sub="${1:-status}"; shift || true
  exec_or_path "$BACKUPCTL" backupctl "$sub" "$@"
}

mount_cmd() {
  local sub="${1:-status}"; shift || true
  exec_or_path "$MOUNTCTL" mountctl "$sub" "$@"
}

hook_cmd() {
  local sub="${1:-status}"; shift || true
  exec_or_path "$HOOKCTL" hookctl "$sub" "$@"
}

gas_cmd() {
  local sub="${1:-help}"; shift || true
  exec_or_path "$GASCTL" gasctl "$sub" "$@"
}

voice_cmd() {
  local sub="${1:-menu}"; shift || true
  exec_or_path "$VOICECTL" voicectl "$sub" "$@"
}

mobileapp_cmd() {
  local sub="${1:-help}"; shift || true
  exec_or_path "$MOBILEAPPCTL" mobileappctl "$sub" "$@"
}

alias_cmd() {
  have aos-alias || die "aos-alias not found in PATH"
  exec aos-alias "${@:-ui}"
}

restart_cmd() {
  local target="${1:-all}"
  [[ -x "$NODE_RESTART" ]] || die "node-restart not found: $NODE_RESTART"
  # Source so we stay in the same process (no exec — we want to return)
  # shellcheck source=scripts/ctl/node-restart.sh
  source "$NODE_RESTART"
  node_restart_main "$target"
}

doctor_tool_line() {
  local label="$1"
  local resolved="${2:-}"
  local fallback="${3:-}"
  local required="${4:-required}"
  local path="${resolved:-}"
  local origin="resolved"
  local state="ok"

  if [[ -z "$path" || ! -x "$path" ]]; then
    if [[ -n "$fallback" ]] && have "$fallback"; then
      path="$(command -v "$fallback")"
      origin="PATH"
    else
      path="-"
      origin="-"
      if [[ "$required" == "optional" ]]; then
        state="optional"
      else
        state="missing"
        AOS_DOCTOR_MISSING=$((AOS_DOCTOR_MISSING + 1))
      fi
    fi
  fi

  printf '  %-12s %-7s %-8s %s\n' "$label" "$state" "$origin" "$path"
}

doctor_cmd() {
  local mode="${1:-local}"
  case "$mode" in
    local|full) ;;
    *)
      die "doctor usage: aos doctor [local|full]"
      ;;
  esac

  local invoked="${0:-aos}"
  local resolved="$invoked"
  resolved="$(readlink -f "$invoked" 2>/dev/null || printf '%s' "$invoked")"

  local tty_stdin=0 tty_stdout=0
  [[ -t 0 ]] && tty_stdin=1
  [[ -t 1 ]] && tty_stdout=1

  AOS_DOCTOR_MISSING=0
  printf 'aos doctor (%s)\n' "$mode"
  printf '  invoked:      %s\n' "$invoked"
  printf '  resolved:     %s\n' "$resolved"
  printf '  script_dir:   %s\n' "$SCRIPT_DIR"
  printf '  tty stdin/out:%s/%s\n' "$tty_stdin" "$tty_stdout"
  printf '  can_prompt:   %s\n' "$(can_prompt && printf yes || printf no)"
  printf '\n'
  printf '  %-12s %-7s %-8s %s\n' "tool" "state" "origin" "path"
  printf '  %-12s %-7s %-8s %s\n' "------------" "-------" "--------" "----"
  doctor_tool_line "aosctl" "$AOSCTL" "aosctl"
  doctor_tool_line "hubctl" "$HUBCTL" "hubctl"
  doctor_tool_line "core4ctl" "$CORE4CTL" "core4ctl"
  doctor_tool_line "gamectl" "$GAMECTL" "gamectl"
  doctor_tool_line "doorctl" "$DOORCTL" "doorctl"
  doctor_tool_line "firectl" "$FIRECTL" "firectl"
  doctor_tool_line "syncctl" "$SYNCCTL" "syncctl"
  doctor_tool_line "gitctl" "$GITCTL" "gitctl"
  doctor_tool_line "nodectl" "$NODECTL" "nodectl"
  doctor_tool_line "indexctl" "$INDEXCTL" "indexctl"
  doctor_tool_line "bridgectl" "$BRIDGECTL" "bridgectl"
  doctor_tool_line "routerctl" "$ROUTERCTL" "routerctl"
  doctor_tool_line "voicectl" "$VOICECTL" "voicectl"
  doctor_tool_line "backupctl" "$BACKUPCTL" "backupctl"
  doctor_tool_line "hookctl" "$HOOKCTL" "hookctl"
  doctor_tool_line "gasctl" "$GASCTL" "gasctl"
  doctor_tool_line "telectl" "$TELECTL" "telectl"
  doctor_tool_line "aos-alias" "$(resolve_bin aos-alias || true)" "aos-alias" optional

  if ((AOS_DOCTOR_MISSING > 0)); then
    printf '\n[aos] missing tools: %d\n' "$AOS_DOCTOR_MISSING" >&2
  else
    printf '\n[aos] local frontdoor wiring OK\n'
  fi

  if [[ "$mode" == "full" ]]; then
    printf '\n[aos] running hub doctor...\n'
    hub_cmd doctor
  fi

  if ((AOS_DOCTOR_MISSING > 0)); then
    return 1
  fi
}

status_cmd() {
  if [[ -n "$AOSCTL" ]]; then
    exec "$AOSCTL" status
  fi
  hub_cmd status
}

ping_http_code() {
  local url="$1"
  if ! have curl; then
    printf "curl?"
    return 0
  fi
  local code
  code="$(curl -sS -o /dev/null -m 1 -w "%{http_code}" "$url" 2>/dev/null || true)"
  if [[ -z "$code" || "$code" == "000" ]]; then
    printf "down"
  else
    printf "%s" "$code"
  fi
}

ping_local_probes() {
  local index_url="http://127.0.0.1:8799/health"
  local bridge_url="http://127.0.0.1:8080/health"
  local index_code bridge_code index_state bridge_state ok_count=0

  index_code="$(ping_http_code "$index_url")"
  bridge_code="$(ping_http_code "$bridge_url")"

  index_state="down"
  bridge_state="down"
  [[ "$index_code" == 2* ]] && index_state="ok"
  [[ "$bridge_code" == 2* ]] && bridge_state="ok"
  [[ "$index_state" == "ok" ]] && ok_count=$((ok_count + 1))
  [[ "$bridge_state" == "ok" ]] && ok_count=$((ok_count + 1))

  printf '[aos] local probes\n'
  printf '  %-8s %-5s %s (%s)\n' "index" "$index_state" "$index_url" "$index_code"
  printf '  %-8s %-5s %s (%s)\n' "bridge" "$bridge_state" "$bridge_url" "$bridge_code"

  ((ok_count > 0))
}

ping_cmd() {
  local mode="${1:-auto}"
  case "$mode" in
    auto|local|remote|full) ;;
    *)
      die "ping usage: aos ping [auto|local|remote|full]"
      ;;
  esac

  local remote_rc=0
  if [[ "$mode" == "auto" || "$mode" == "remote" || "$mode" == "full" ]]; then
    local remote_out=""
    if [[ -n "$AOSCTL" ]]; then
      if remote_out="$("$AOSCTL" ping 2>&1)"; then
        remote_rc=0
      else
        remote_rc=$?
      fi
    else
      if remote_out="$(aosctl ping 2>&1)"; then
        remote_rc=0
      else
        remote_rc=$?
      fi
    fi

    if ((remote_rc == 0)); then
      printf '[aos] remote heartbeat ok\n'
      if [[ "$mode" != "full" ]]; then
        return 0
      fi
    else
      printf '[aos] remote heartbeat failed (rc=%d)\n' "$remote_rc" >&2
      if [[ "${AOS_PING_VERBOSE:-0}" == "1" && -n "$remote_out" ]]; then
        printf '%s\n' "$remote_out" >&2
      fi
      if [[ "$mode" == "remote" ]]; then
        return "$remote_rc"
      fi
    fi
  fi

  if ping_local_probes; then
    return 0
  fi

  if [[ "$mode" == "local" ]]; then
    return 1
  fi

  if ((remote_rc == 0)); then
    return 0
  fi
  return 1
}

case "$cmd" in
  dashboard|dash|ui) dashboard ;;
  menu) menu ;;
  status) status_cmd ;;
  ping) ping_cmd "$@" ;;
  doctor) doctor_cmd "$@" ;;
  restart) restart_cmd "$@" ;;
  sync) sync_cmd "$@" ;;
  git) git_cmd "$@" ;;
  vault) vault_cmd "$@" ;;
  hub) hub_cmd "$@" ;;
  aosctl) aosctl_cmd "$@" ;;
  core4) core4_cmd "$@" ;;
  index) index_cmd "$@" ;;
  node|nodectl) node_cmd "$@" ;;
  bridge) bridge_cmd "$@" ;;
  router) router_cmd "$@" ;;
  game) game_cmd "$@" ;;
  frame) frame_cmd "$@" ;;
  freedom) freedom_cmd "$@" ;;
  focus) focus_centre_cmd "$@" ;;
  tent) tent_centre_cmd "$@" ;;
  door) door_cmd "$@" ;;
  hot) hot_cmd "$@" ;;
  potential|plan|production|profit) door_phase_cmd "$cmd" "$@" ;;
  fire) fire_cmd "$@" ;;
  tele) tele_cmd "$@" ;;
  backup) backup_cmd "$@" ;;
  mount) mount_cmd "$@" ;;
  hook) hook_cmd "$@" ;;
  gas) gas_cmd "$@" ;;
  voice) voice_cmd "$@" ;;
  mobileapp|mobileappctl|pwa) mobileapp_cmd "$@" ;;
  alias) alias_cmd "$@" ;;
  help|-h|--help) usage ;;
  "")
    if can_prompt; then
      dashboard
    else
      usage
    fi
    ;;
  *)
    echo "[$APP] Unknown command: $cmd" >&2
    usage
    exit 1
    ;;
esac
