# αOS Hub (systemd system units) env
#
# Install to: /etc/alphaos-hub/env
#
# Used by:
# - aos-index.service
# - aos-router.service
# - aos-bridge.service
# - alphaos-vault-sync-{pull,push}.service

# Absolute path to your aos-hub checkout
AOS_HUB_DIR=/home/alpha/αOS/aos-hub

# Directory that holds runtime env files (router/bridge/index)
AOS_ENV_DIR=/home/alpha/.env

# Optional: env for vault-sync units
# alphaos-vault-sync.env can live in AOS_ENV_DIR and override VAULT_REMOTE/VAULT_REMOTE_BACKUP.

# NOTE:
# systemd does not expand variables inside unit paths (EnvironmentFile/WorkingDirectory),
# so the system units read additional env files from fixed paths under:
#   /etc/alphaos-hub/*.env
#
# Suggested setup:
#   sudo ln -sf "$AOS_ENV_DIR/router.env" /etc/alphaos-hub/router.env
#   sudo ln -sf "$AOS_ENV_DIR/bridge.env" /etc/alphaos-hub/bridge.env
#   sudo ln -sf "$AOS_ENV_DIR/telegram.env" /etc/alphaos-hub/telegram.env
#   sudo ln -sf "$AOS_ENV_DIR/alphaos-index.env" /etc/alphaos-hub/alphaos-index.env

# Optional UI basic auth (set in /etc/alphaos-hub/alphaos-index.env):
#   AOS_UI_BASIC_AUTH=1
#   AOS_UI_BASIC_USER=alpha
#   AOS_UI_BASIC_PASS=change-me
#   AOS_UI_BASIC_REALM=AlphaOS Hub
#   # Optional: also protect /api/* (default is 0 = APIs stay open)
#   AOS_UI_BASIC_AUTH_PROTECT_API=0
