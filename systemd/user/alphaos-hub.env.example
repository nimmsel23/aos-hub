# αOS Hub env (systemd user units)
#
# Install to: ~/.env/alphaos-hub.env
#
# Used by:
# - systemd/user/aos-bridge.service (via bridgectl)

# Absolute path to your aos-hub checkout
AOS_HUB_DIR=/home/alpha/αOS/aos-hub

